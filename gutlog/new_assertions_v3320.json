{
 "_": ["GutLog v3.32.0 -- the food test. v3.31.0 has no food test at all, so every assertion fails on it and that says nothing about the promises. Each promise is therefore also broken on purpose: the confounder flag hidden (the brief's own requirement), a skipped day moving the plan on, the stop applied without asking, a re-timed dose leaving its meal behind, a score saved without all four fields, the first washout day missing from the results, a step out of order accepted, a second step on one day accepted, a stop that does not move to washout, the typed amount ignored, a limit without grams accepted, and the card too wide for the folded screen."],
 "suite": "test_v3320_foodtest.py",
 "app": "app.py",
 "patcher": "patch_gutlog_v3320_foodtest.py",
 "assertions": [
  {"name": "01 the plan seeds with its ladders and units", "control": "version"},
  {"name": "02 the next step follows what was logged", "control": "version"},
  {"name": "03 a skipped day does not move the plan on", "control": "version"},
  {"name": "03 a skipped day does not move the plan on", "control": "mutation",
   "mutation": {"id": "skipadvances", "why": "a skipped day counts as a step, so the plan moves on by the calendar",
    "find": "    done = set(r[\"step\"] for r in rows if r[\"kind\"] in FT_STEP_KINDS and r[\"step\"] is not None)",
    "replace": "    done = set(range(len([r for r in rows if r[\"kind\"] in FT_STEP_KINDS + (\"skip\",)])))"}},
  {"name": "04 a pause holds the plan where it was", "control": "version"},
  {"name": "05 a step logged on a past day and re-timed carries the day totals", "control": "version"},
  {"name": "05 a step logged on a past day and re-timed carries the day totals", "control": "mutation",
   "mutation": {"id": "mealstays", "why": "re-timing a dose leaves its meal, and the day's totals, on the old day",
    "find": "    if tbl[0] == \"ft_log\" and row[\"meal_id\"]:",
    "replace": "    if False:"}},
  {"name": "06 the evening score needs all four and re-opens for editing", "control": "version"},
  {"name": "06 the evening score needs all four and re-opens for editing", "control": "mutation",
   "mutation": {"id": "partialscore", "why": "a score with a field missing is saved",
    "find": "    if missing:\n        return jsonify(ok=False, err=\"Fill in all four: \"",
    "replace": "    if False:\n        return jsonify(ok=False, err=\"Fill in all four: \""}},
  {"name": "07 clear symptoms offer the stop; taking it records the limit and washout", "control": "version"},
  {"name": "07 clear symptoms offer the stop; taking it records the limit and washout", "control": "mutation",
   "mutation": {"id": "autostop", "why": "the stop rule is imposed from the score instead of offered",
    "find": "    return jsonify(ok=True, id=sid, washout=washed,",
    "replace": "    _o = ft_offer_stop(day) if clear else None\n    if _o:\n        db().execute(\"INSERT OR REPLACE INTO ft_outcome(slug, outcome, limit_g, set_at) VALUES(?,?,?,?)\", (_o[\"slug\"], \"limit\", _o[\"limit_g\"], now_s()))\n        db().commit()\n    return jsonify(ok=True, id=sid, washout=washed,"}},
  {"name": "07 clear symptoms offer the stop; taking it records the limit and washout", "control": "mutation",
   "mutation": {"id": "nostopjump", "why": "after a stop the ladder carries on with the next dose",
    "find": "(cur in done or (stopped and steps[cur][\"kind\"] == \"dose\"))",
    "replace": "(cur in done or (False and steps[cur][\"kind\"] == \"dose\"))"}},
  {"name": "08 a medicine change inside a challenge week raises the flag", "control": "version"},
  {"name": "08 a medicine change inside a challenge week raises the flag", "control": "mutation",
   "mutation": {"id": "hideflag", "why": "the confounder flag is hidden",
    "find": "        if n:\n            flags.append(\"medicine change",
    "replace": "        if False:\n            flags.append(\"medicine change"}},
  {"name": "09 results show the challenge days plus the first washout day", "control": "version"},
  {"name": "09 results show the challenge days plus the first washout day", "control": "mutation",
   "mutation": {"id": "nowashday", "why": "the results stop at the last dose, though reactions run 24-48 h behind",
    "find": "        if first_wash and doses:",
    "replace": "        if False:"}},
  {"name": "10 only the next step can be logged, once a day", "control": "version"},
  {"name": "10 only the next step can be logged, once a day", "control": "mutation",
   "mutation": {"id": "anystep", "why": "any step can be logged, out of order",
    "find": "    if it is None or it[\"slug\"] != d.get(\"slug\") or cur != d.get(\"step\"):",
    "replace": "    if it is None or it[\"slug\"] != d.get(\"slug\"):"}},
  {"name": "10 only the next step can be logged, once a day", "control": "mutation",
   "mutation": {"id": "twoaday", "why": "two steps of one week can be logged on the same day",
    "find": "    if any(r[\"day\"] == day and r[\"kind\"] in FT_STEP_KINDS for r in rows):\n        return jsonify(ok=False, err=\"A step of this week",
    "replace": "    if False:\n        return jsonify(ok=False, err=\"A step of this week"}},
  {"name": "11 an outcome is his to set and to change", "control": "version"},
  {"name": "11 an outcome is his to set and to change", "control": "mutation",
   "mutation": {"id": "limitnog", "why": "a limit is recorded without its grams",
    "find": "        if not lim or lim <= 0:",
    "replace": "        if False:"}},
  {"name": "12 the card shows the next step after a day is logged", "control": "version"},
  {"name": "13 the card holds the step after a skipped day and while paused", "control": "version"},
  {"name": "13 the card holds the step after a skipped day and while paused", "control": "mutation",
   "mutation": {"id": "skipadvances", "why": "a skipped day counts as a step, so the plan moves on by the calendar",
    "find": "    done = set(r[\"step\"] for r in rows if r[\"kind\"] in FT_STEP_KINDS and r[\"step\"] is not None)",
    "replace": "    done = set(range(len([r for r in rows if r[\"kind\"] in FT_STEP_KINDS + (\"skip\",)])))"}},
  {"name": "14 the amount and time can be changed before Taken", "control": "version"},
  {"name": "14 the amount and time can be changed before Taken", "control": "mutation",
   "mutation": {"id": "amountignored", "why": "the card logs the planned amount whatever he typed",
    "find": "    if(cu.kind==='dose')body.amount=$('#ftAmt').value;",
    "replace": "    if(cu.kind==='dose')body.amount=cu.g;"}},
  {"name": "15 the card offers the stop and records it only on a tap", "control": "version"},
  {"name": "15 the card offers the stop and records it only on a tap", "control": "mutation",
   "mutation": {"id": "autostop", "why": "the stop rule is imposed from the score instead of offered",
    "find": "    return jsonify(ok=True, id=sid, washout=washed,",
    "replace": "    _o = ft_offer_stop(day) if clear else None\n    if _o:\n        db().execute(\"INSERT OR REPLACE INTO ft_outcome(slug, outcome, limit_g, set_at) VALUES(?,?,?,?)\", (_o[\"slug\"], \"limit\", _o[\"limit_g\"], now_s()))\n        db().commit()\n    return jsonify(ok=True, id=sid, washout=washed,"}},
  {"name": "16 the results page reads right and fits the folded screen", "control": "version"},
  {"name": "16 the results page reads right and fits the folded screen", "control": "mutation",
   "mutation": {"id": "hideflag", "why": "the confounder flag is hidden",
    "find": "        if n:\n            flags.append(\"medicine change",
    "replace": "        if False:\n            flags.append(\"medicine change"}},
  {"name": "16 the results page reads right and fits the folded screen", "control": "mutation",
   "mutation": {"id": "widecard", "why": "the amount box is wider than the folded screen, so the card runs off it",
    "find": ".ftrow input[type=number]{width:84px;flex:0 0 84px;padding:8px}",
    "replace": ".ftrow input[type=number]{width:384px;flex:0 0 384px;padding:8px}"}}
 ]
}
