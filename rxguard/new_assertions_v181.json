{
 "_": [
  "RxGuard v1.8.1 -- label maxima, no confirming. 10 and 14 fail on v1.8.0 (Confirm button, 'default' chip, a cleared box meant 'no ceiling'). 15 is declared twice: once by breaking what app.py contributes (the 10-day feed), once by breaking the course rule inside the dose_ceiling.py sidecar -- NEGATIVE_CONTROL now mutates a sidecar in a copied folder, never the real file."
 ],
 "suite": "test_dose_ceiling.py",
 "app": "app.py",
 "patcher": "patch_rxguard_v181.py",
 "assertions": [
  {
   "name": "10 his own limit overrides; clearing returns to the label maximum",
   "control": "version"
  },
  {
   "name": "14 a ceiling is the label maximum, with its source, never 'confirm'",
   "control": "version"
  },
  {
   "name": "15 a short-course dose taken day after day is named",
   "control": "mutation",
   "mutation": {
    "id": "shortfeed",
    "why": "the dose feed looks back no further than today, so no run can be seen",
    "find": "DOSE_FEED_DAYS = 10\n",
    "replace": "DOSE_FEED_DAYS = 0\n"
   }
  },
  {
   "name": "15 a short-course dose taken day after day is named",
   "control": "mutation",
   "mutation": {
    "id": "courserun",
    "module": "dose_ceiling.py",
    "why": "the course rule never counts a day, so no run is ever long enough",
    "find": "                run += 1\n                d -= timedelta(days=1)\n",
    "replace": "                run += 0\n                d -= timedelta(days=1)\n"
   }
  }
 ]
}
